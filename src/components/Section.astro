---
interface Props {
  id?: string
  subtitle: string
}

const { id, subtitle } = Astro.props

/*´${id_name ? id=${id_name} : {}}´*/
---

<section {...id ? { id } : {}} class="w-full min-h-fit py-4 px-8 md:py-6 md:px-16 lg:py-8 lg:px-24">
  <h2 class="mb-4 text-white text-3xl md:text-4xl lg:text-5xl font-normal font-jersey leading-none">{subtitle}</h2>
  <slot />
</section>
